# TODO_页面统一优化

## 待办事项和建议

1. **安全性建议**：
   - ⚠️ **请立即登录后台修改默认密码**。当前密码 `admin123` 仅供恢复访问使用，存在安全风险。
   - 修改路径：后台管理 -> 系统设置 -> 管理员列表 -> 修改密码。

2. **图片替换建议**：
   - **Banner 图片**：建议使用分辨率为 `1920x822` (或保持 21:9 比例) 的图片，以获得最佳视觉效果。
   - **文化/介绍图片**：建议保持约 `2.5:1` 的比例。
   - 替换方式：后台管理 -> 栏目管理 -> 点击对应栏目的“修改” -> 在“上传 Banner”处上传新图片。

3. **移动端适配**：
   - 当前优化主要针对桌面端（宽屏）。虽然 `custom.css` 中包含部分响应式代码，但建议在手机端实际查看，如需微调可联系我。

4. **清理临时文件**：
   - 任务完成后，可以删除根目录下的 `reset_pwd.php` 文件以防安全隐患。
